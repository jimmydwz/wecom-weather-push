name: Weather to WeCom

on:
  schedule:
    - cron: "0 1 * * *" # 09:00 北京时间（UTC 01:00）
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    env:
      WECOM_WEBHOOK: ${{ secrets.WECOM_WEBHOOK }}
    steps:
      - name: Install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Fetch weather and push
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${WECOM_WEBHOOK:-}" ]; then
            echo "Missing secret WECOM_WEBHOOK"
            exit 2
          fi

          # weathercode -> 中文描述
          weather_desc () {
            case "$1" in
              0) echo "晴朗" ;;
              1) echo "大部晴朗" ;;
              2) echo "多云" ;;
              3) echo "阴天" ;;
              45|48) echo "有雾" ;;
              51) echo "小毛毛雨" ;;
              53) echo "中等毛毛雨" ;;
              55) echo "大毛毛雨" ;;
              56|57) echo "冻毛毛雨" ;;
              61) echo "小雨" ;;
              63) echo "中雨" ;;
              65) echo "大雨" ;;
              66|67) echo "冻雨" ;;
              71) echo "小雪" ;;
              73) echo "中雪" ;;
              75) echo "大雪" ;;
              77) echo "雪粒" ;;
              80) echo "小阵雨" ;;
              81) echo "中阵雨" ;;
              82) echo "强阵雨" ;;
              85) echo "小阵雪" ;;
              86) echo "大阵雪" ;;
              95) echo "雷暴" ;;
              96|99) echo "雷暴伴冰雹" ;;
              *) echo "未知天气" ;;
            esac
          }

          # === New York ===
          ny_json="$(curl -fsS "https://api.open-meteo.com/v1/forecast?latitude=40.7128&longitude=-74.0060&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max,weathercode&timezone=America/New_York&forecast_days=2")"

          ny_d0="$(jq -r '.daily.time[0]' <<<"$ny_json")"
          ny_d1="$(jq -r '.daily.time[1]' <<<"$ny_json")"
          ny_tmax0="$(jq -r '.daily.temperature_2m_max[0]' <<<"$ny_json")"
          ny_tmin0="$(jq -r '.daily.temperature_2m_min[0]' <<<"$ny_json")"
          ny_pop0="$(jq -r '.daily.precipitation_probability_max[0] // 0' <<<"$ny_json")"
          ny_code0="$(jq -r '.daily.weathercode[0]' <<<"$ny_json")"

          ny_tmax1="$(jq -r '.daily.temperature_2m_max[1]' <<<"$ny_json")"
          ny_tmin1="$(jq -r '.daily.temperature_2m_min[1]' <<<"$ny_json")"
          ny_pop1="$(jq -r '.daily.precipitation_probability_max[1] // 0' <<<"$ny_json")"
          ny_code1="$(jq -r '.daily.weathercode[1]' <<<"$ny_json")"

          ny_desc0="$(weather_desc "$ny_code0")"
          ny_desc1="$(weather_desc "$ny_code1")"

          # === Los Angeles ===
          la_json="$(curl -fsS "https://api.open-meteo.com/v1/forecast?latitude=34.0522&longitude=-118.2437&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max,weathercode&timezone=America/Los_Angeles&forecast_days=2")"

          la_d0="$(jq -r '.daily.time[0]' <<<"$la_json")"
          la_d1="$(jq -r '.daily.time[1]' <<<"$la_json")"
          la_tmax0="$(jq -r '.daily.temperature_2m_max[0]' <<<"$la_json")"
          la_tmin0="$(jq -r '.daily.temperature_2m_min[0]' <<<"$la_json")"
          la_pop0="$(jq -r '.daily.precipitation_probability_max[0] // 0' <<<"$la_json")"
          la_code0="$(jq -r '.daily.weathercode[0]' <<<"$la_json")"

          la_tmax1="$(jq -r '.daily.temperature_2m_max[1]' <<<"$la_json")"
          la_tmin1="$(jq -r '.daily.temperature_2m_min[1]' <<<"$la_json")"
          la_pop1="$(jq -r '.daily.precipitation_probability_max[1] // 0' <<<"$la_json")"
          la_code1="$(jq -r '.daily.weathercode[1]' <<<"$la_json")"

          la_desc0="$(weather_desc "$la_code0")"
          la_desc1="$(weather_desc "$la_code1")"

          content="$(printf "### 🌤️ 纽约 & 洛杉矶天气预报（今天/明天）\n\n**纽约 New York**\n- 今天（%s）：%s；%s°C / %s°C；降水 %s%%\n- 明天（%s）：%s；%s°C / %s°C；降水 %s%%\n\n**洛杉矶 Los Angeles**\n- 今天（%s）：%s；%s°C / %s°C；降水 %s%%\n- 明天（%s）：%s；%s°C / %s°C；降水 %s%%\n" \
            "$ny_d0" "$ny_desc0" "$ny_tmax0" "$ny_tmin0" "$ny_pop0" \
            "$ny_d1" "$ny_desc1" "$ny_tmax1" "$ny_tmin1" "$ny_pop1" \
            "$la_d0" "$la_desc0" "$la_tmax0" "$la_tmin0" "$la_pop0" \
            "$la_d1" "$la_desc1" "$la_tmax1" "$la_tmin1" "$la_pop1")"

          payload="$(jq -n --arg c "$content" '{msgtype:"markdown", markdown:{content:$c}}')"
          resp="$(curl -fsS -X POST "$WECOM_WEBHOOK" -H "Content-Type: application/json" -d "$payload")"
          echo "WeCom response: $resp"
